
# 📘 دليل إدارة موقع TechTouch (المرجع الشامل)

أهلاً بك في الدليل التشغيلي لموقع **TechTouch**. هذا الموقع مبني بنظام **Static Website** (ملفات ثابتة HTML/CSS/JS)، مما يجعله سريعاً جداً وآمناً.

---

## 🚀 لوحة التحكم (CMS) - المميزات الجديدة

يمكنك الآن إدارة الموقع بسهولة عبر لوحة التحكم الموجودة في المجلد `/admin`.

### 1. إضافة فيديوهات وروابط في المقالات
عند كتابة مقال جديد، ستجد أزراراً مساعدة فوق صندوق المحتوى:
*   **زر يوتيوب (Youtube):** اضغط عليه، ثم الصق رابط الفيديو (مثلاً: `https://youtube.com/watch?v=xxxxx`). سيقوم النظام تلقائياً بتحويله إلى مشغل فيديو يعمل داخل الموقع.
*   **زر رابط (Link):** اضغط عليه لإضافة زر تحميل أو انتقال. سيطلب منك الرابط (URL) والنص الذي سيظهر (مثل "اضغط هنا للتحميل").

### 2. تعديل معلومات "من نحن" والبوت
*   اذهب إلى تبويب **"الملف الشخصي والمظهر"** في لوحة التحكم.
*   ستجد أقساماً جديدة:
    *   **معلومات بوت الطلبات:** اكتب التعليمات التي ستظهر للمستخدمين (كل نقطة في سطر).
    *   **طرق البحث:** اكتب نصائح البحث في القنوات (كل نصيحة في سطر).
*   اضغط "حفظ التغييرات" ليتم تحديث ملف `about.json`.

---

## 📑 القسم الأول: الإدارة اليدوية (بدون لوحة تحكم)

إذا كنت تفضل تعديل الملفات يدوياً مباشرة:

### 1. خطوات نشر مقال جديد
1.  **إنشاء الملف:** انسخ ملف `article-asus-gx10.html` وأعد تسميته.
2.  **تعديل المحتوى:** عدل العنوان والتفاصيل.
3.  **تحديث القوائم:** أضف المقال يدوياً في `index.html` و `articles.html`.

### 2. صور المنشورات
*   المسار: `assets/images/`

---

## 🛠️ القسم الثاني: دليل القنوات والأدوات

*   **القنوات:** يتم تعديلها من لوحة التحكم (تبويب القنوات)، أو يدوياً عبر ملف `content/data/channels.json`.
*   **الهواتف:** يتم تعديل بياناتها في `assets/js/phones-data.js`.

---

## 📍 أوامر التشغيل (للمطورين)

بعد إجراء التغييرات من لوحة التحكم أو يدوياً في ملفات `content/`، يجب إعادة بناء الموقع ليتم تحديث صفحات HTML.

1.  افتح الطرفية (Terminal).
2.  نفذ الأمر:
    ```bash
    npm run build
    ```
    *أو*
    ```bash
    node scripts/generate.js
    ```
3.  سيقوم هذا السكربت بقراءة ملفات JSON وتحديث ملفات HTML (index, about, articles) تلقائياً.

